#include "main.h"

/**
 * _isdigit - Check if the a given argument is a digit.
 * @ch: inpt parameter
 * Return: 1 to indicate success and 0 otherwise.
 */
int _isdigit(int ch)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
